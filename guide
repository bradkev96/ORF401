• "push": actually upload your changes to the shared repository
• "commit": create a digital checkpoint of your changes so far
• "pull": get the most recent copy


HOW TO PUSH:
git status
git add .
git commit -m “Add README.md”
git pull
git push -u origin master

HOW TO GET RECENT COPY:
git pull

HOW TO UNDO CHANGES TO A FILE:
git checkout [filename]